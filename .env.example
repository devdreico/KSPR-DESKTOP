# Required for Docker/API mode. Generate with: openssl rand -hex 32
KSPR_API_TOKEN=replace-with-a-long-random-token
KSPR_JWT_SECRET_KEY=replace-with-another-long-random-secret

# Public URL of the separately deployed backend used by the Vercel frontend.
VITE_API_URL=https://your-backend.example.com
